## Packages
framer-motion | Smooth animations for training progress and UI transitions
date-fns | Date formatting for model creation times
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
- Expecting a standard dark mode environment.
- Polling is implemented via TanStack Query `refetchInterval` for live training updates.
- Wouter is used for routing.
